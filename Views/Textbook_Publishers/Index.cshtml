@model IEnumerable<TextbookFinder.Models.Textbook_Publishers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Publisher_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Publisher_website)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher_website)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Publisher_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Publisher_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Publisher_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
